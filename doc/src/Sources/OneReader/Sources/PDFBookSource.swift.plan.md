# `Sources/OneReader/Sources/PDFBookSource.swift`

Owns PDF byte loading, SHA-256 revision calculation, PDFKit document creation,
outline traversal, and outline-free page grouping.

It does not infer semantic claims. PDF bytes selected locally stay on device.

